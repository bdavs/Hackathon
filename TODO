add fields to all files (classroom, time, etc.)
make each file into functions
create interface for entering new events
	allow users to edit/update existing events
	dynamically add as many events as needed
make Current event more prominent on window
make grid dynamically display all future events
create database in back end


